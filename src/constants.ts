export const SCREEN_WIDTH = window.innerWidth;
export const SCREEN_HEIGHT = window.innerHeight;
export const aspect = SCREEN_WIDTH / SCREEN_HEIGHT;
export const GRID_SIZE = 30;
export const INVADER_ROWS = 6;
export const INVADER_COLUMNS = 10;
export const INVADER_SPACING = 5;
export const DEBUG = false;
export const PLAYER_START_Y = -30;

// Sky constants, stars and planets
export const SKY = {
  STARS_AMOUNT: 5000,
  RADIUS: 150,
  MAX_DISTANCE: 5
}